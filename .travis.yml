language: python
python:
  # We don't actually use the Travis Python, but this keeps it organized.
  - "3.6"
  - "2.7"
git:
  depth: 1
branches:
  only:
  - master
services:
  - docker
script:
  - python setup.py tests